package xsrf

// import xsrf "golang.org/x/net/xsrftoken"

// tok := xsrf.Generate(hmacSecret, s.setSession(w, r), "")
// 	ok := xsrf.Valid(r.PostFormValue("_xsrf"), hmacSecret, s.getSession(w, r), "")
